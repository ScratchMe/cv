{
  "name": "cv-antoine-berthaud",
  "private": true,
  "description": "Site CV — génération des PDF FR/EN via Playwright",
  "scripts": {
    "generate-static": "node scripts/generate-static.js",
    "generate-pdf": "node scripts/generate-pdf.js",
    "generate": "npm run generate-static && npm run generate-pdf"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "playwright": "^1.56.0"
  }
}
